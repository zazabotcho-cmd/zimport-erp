WINGLOBAL PROGRAM — LOCAL FILE MODE

Open index.html directly from Windows using file:///.

Local file mode:
- does not show the Supabase login overlay;
- keeps buttons and editing enabled;
- uses browser local storage;
- skips cloud synchronization while opened with file://.

When hosted over HTTP/HTTPS, normal Supabase behavior remains enabled.
